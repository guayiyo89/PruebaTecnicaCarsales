import { environment } from "src/environments/environment";

export const URLS = {
    characters: `${environment.urlBase}/character`,
    locations: `${environment.urlBase}/location`,
    episode: `${environment.urlBase}/episode`,
}